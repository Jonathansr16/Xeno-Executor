import { PipegalleryPipe } from './pipegallery.pipe';

describe('PipegalleryPipe', () => {
  it('create an instance', () => {
    const pipe = new PipegalleryPipe();
    expect(pipe).toBeTruthy();
  });
});
